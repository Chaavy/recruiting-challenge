# Task index

Rows are in execution order. The next task is the first row with status `pending`. Statuses: `pending`, `in-progress`, `done`. See `README.md` for the lifecycle.

| ID | Title | Type | Status | Spec |
|----|-------|------|--------|------|

No tasks defined yet. The task contracts are created in step JS-003 (project tasks final definition, see `docs/personal-notes.md`) after the architecture review session. Candidates from the notes, in priority order: CI golden gate, security vulnerabilities, revenue frontier fix, order-event webhooks.
